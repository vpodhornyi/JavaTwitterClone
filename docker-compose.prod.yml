version: "3.9"

services:
  twitter-frontend:
    image: node:22.15.0
    container_name: twitter-frontend
    working_dir: /app
    volumes:
      - ./client:/app
    command: sh -c "npm install && npm run build && npx serve -s dist -l 8081"
    ports:
      - "8081:8081"
    networks:
      - proxy
      - twitter-net

  twitter-db:
    image: postgres:17
    container_name: twitter-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - twitter_db_data:/var/lib/postgresql/data
    networks:
      - twitter-net

  twitter-rabbit:
    image: pcloud/rabbitmq-stomp:latest
    container_name: twitter-rabbit
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    ports:
      - "5672:5672"    # AMQP для Spring
      - "15672:15672"  # веб-панель RabbitMQ
      - "61613:61613"  # STOMP (то, что нужно WebSocketConfig)
    volumes:
      - twitter-rabbit-data:/var/lib/rabbitmq/mnesia
    networks:
      - twitter-net

  twitter-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: twitter-backend
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - twitter-db
      - twitter-rabbit
    networks:
      - twitter-net
      - proxy

networks:
  twitter-net:
  proxy:
    external: true

volumes:
  twitter_db_data:
    external: false
  twitter-rabbit-data:
    external: false